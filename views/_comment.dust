<div chapter_index="{.chapter_index}" slide_index="{.slide_index}" class="item_comment">
  <p><span class="user">{.user.name},</span> <span class="time">{.nice_time}</span> 
  {?slide_title}
  on slide <a href="javascript:void(0)" class="slide_title">"{.slide_title}"</a>
  {:else}
  on presentation
  {/slide_title}</p>
  <div class="text_comment">{.text|s|h|pn}</div>
</div>
